services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile.client
    container_name: auradb-knowledge-graph
    ports:
      - "3000:3000"
    env_file:
      - ../.env
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
